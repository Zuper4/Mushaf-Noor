# Similar instructions for Warsh qiraat

Replace this with your `mushaf_warsh.pdf` file.

**Note**: Warsh qiraat may have different page numbers compared to Hafs.
Update the page mapping in `PDFService.getSurahStartPage()` method accordingly.